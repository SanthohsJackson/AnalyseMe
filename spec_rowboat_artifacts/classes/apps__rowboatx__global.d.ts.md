# apps/rowboatx/global.d.ts

**File:** apps/rowboatx/global.d.ts  
**Language:** typescript

## Purpose
Extend the global Window interface to include a config object with an apiBase string.

## Interfaces
### `Window` (class)
```
interface Window
```
**Intent:** Augment the Window interface to provide a configuration object for API base URL.

