# apps/rowboat/src/entities/errors/job-errors.ts

**File:** apps/rowboat/src/entities/errors/job-errors.ts  
**Language:** typescript

## Purpose
Define a custom error class for job acquisition failures.

## Interfaces
### `JobAcquisitionError` (method)
```
constructor(message?: string, options?: ErrorOptions)
```
**Intent:** Initialize a new instance of JobAcquisitionError with an optional message and options.

**Inputs:**
- message?: string — optional error message
- options?: ErrorOptions — optional error options
