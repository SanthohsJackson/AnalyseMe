# apps/x/packages/core/src/application/browser-control/service.ts

**File:** apps/x/packages/core/src/application/browser-control/service.ts  
**Language:** typescript

## Purpose
Defines an interface for a browser control service that executes a command with optional context.

## Interfaces
### `IBrowserControlService` (class)
```
IBrowserControlService
```
**Intent:** Represents a contract for executing browser control commands with optional context.


## External dependencies
- @x/shared/dist/browser-control.js
