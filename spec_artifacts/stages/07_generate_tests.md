# Stage: Test Generation

**Acceptance tests:** 151

### pipeline/nodes/validate.validate
- **Given:** A PipelineState object with unit analyses and a draft document.
- **When:** The validate function is called with the PipelineState.
- **Then:** A dictionary is returned containing validation results, gaps, and the final document.

### pipeline/nodes/validate.validate
- **Given:** A PipelineState object with unit analyses and a draft document.
- **When:** The validate function is called and the LLM call fails.
- **Then:** An Exception is raised.

### pipeline/reflection.reflect_on_run
- **Given:** A repository name, a list of programming languages, a unit count, and a list of hallucinations.
- **When:** The reflect_on_run function is called with these inputs.
- **Then:** A string is returned containing a concise reflection on the analysis.

### pipeline/nodes/synthesize_system.synthesize_system
- **Given:** A PipelineState object with module summaries and metadata.
- **When:** The synthesize_system function is called with the PipelineState.
- **Then:** A dictionary is returned containing the system overview generated by the LLM.

### pipeline/rag.__init__
- **Given:** No preconditions.
- **When:** The RAGIndex is initialized.
- **Then:** An in-memory vector store for embeddings is initialized.

### pipeline/rag.add
- **Given:** A list of (id, text) tuples.
- **When:** The add method is called with the list of tuples.
- **Then:** The documents are added to the in-memory vector store.

### pipeline/rag.retrieve
- **Given:** A query string and an integer k.
- **When:** The retrieve method is called with the query and k.
- **Then:** A list of the top-k documents matching the query is returned.

### pipeline/rag.select_context
- **Given:** Full text, a list of records, a query string, a character budget, and an integer k.
- **When:** The select_context function is called with these inputs.
- **Then:** A tuple is returned containing the context text and a flag indicating if RAG was used.

### pipeline/cache.enabled
- **Given:** No preconditions.
- **When:** The enabled function is called.
- **Then:** A boolean is returned indicating whether the cache is enabled.

### pipeline/cache.cache_dir
- **Given:** No preconditions.
- **When:** The cache_dir function is called.
- **Then:** A Path object is returned representing the cache directory, and the directory is created if it does not exist.

### pipeline/cache.make_key
- **Given:** A set of string parts.
- **When:** The make_key function is called with the parts.
- **Then:** A SHA-256 hash string is returned representing the cache key.

### pipeline/cache.get
- **Given:** A cache key string.
- **When:** The get function is called with the key.
- **Then:** The cached value is returned if available, otherwise None.

### pipeline/cache.set
- **Given:** A cache key string and a JSON-serializable value.
- **When:** The set function is called with the key and value.
- **Then:** The value is stored in the cache under the specified key.

### pipeline/cache.clear
- **Given:** No preconditions.
- **When:** The clear function is called.
- **Then:** All entries are removed from the cache, and the number of files removed is returned.

### pipeline/nodes/generate_tests.generate_tests
- **Given:** A PipelineState object with unit analyses.
- **When:** The generate_tests function is called with the PipelineState.
- **Then:** A dictionary is returned containing generated behavioral tests.

### pipeline/nodes/route_units.route_units
- **Given:** A PipelineState object.
- **When:** The route_units function is called with the PipelineState.
- **Then:** A dictionary is returned containing units to analyze, and the number of units queued is printed.

### pipeline/repo_source.is_git_url
- **Given:** A source string.
- **When:** The is_git_url function is called with the source.
- **Then:** A boolean is returned indicating whether the source is a valid git URL.

### pipeline/repo_source.resolve_repo_source
- **Given:** A source string, a logging function, and a force_fresh boolean.
- **When:** The resolve_repo_source function is called with these inputs.
- **Then:** A string is returned representing the local directory path for the resolved source.

### pipeline/nodes/review_completeness.review_completeness
- **Given:** A PipelineState object with unit analyses and repo path.
- **When:** The review_completeness function is called with the PipelineState.
- **Then:** A dictionary is returned containing analysis questions, completeness status, and pass number.

### cli/chat.chat_repl
- **Given:** A collection name and an integer k.
- **When:** The chat_repl function is called with these inputs.
- **Then:** An interactive Q&A loop is run, querying an indexed collection with a local LLM.

### pipeline/indexer.index_run
- **Given:** A repository path, a dictionary of class documentation, a specification text, and a logging function.
- **When:** The index_run function is called with these inputs.
- **Then:** A tuple is returned containing the collection name and document count.

### pipeline/nodes/reduce_modules.reduce_modules
- **Given:** A PipelineState object with unit analyses.
- **When:** The reduce_modules function is called with the PipelineState.
- **Then:** A dictionary is returned containing module summaries.

### pipeline/memory.store_run
- **Given:** A repository path, a model string, a list of languages, a unit count, a list of hallucinations, and optional reflection notes.
- **When:** The store_run method is called with these inputs.
- **Then:** A new entry is written to the markdown memory file, rotating entries if necessary.

### pipeline/memory.py._load_blocks
- **Given:** The memory file contains log entries.
- **When:** _load_blocks is called.
- **Then:** A list of log entries is returned.

### pipeline/memory.py.get_past_context
- **Given:** A repository path and a number of past entries to consider are provided.
- **When:** get_past_context is called with the repository path and number of entries.
- **Then:** A formatted string of lessons from past runs is returned.

### pipeline/memory.py._rotate
- **Given:** A list of log entries is provided.
- **When:** _rotate is called with the list of log entries.
- **Then:** A rotated list of log entries is returned, ensuring the number does not exceed the maximum allowed.

### graph.py.route_to_units
- **Given:** A PipelineState object is provided.
- **When:** route_to_units is called with the PipelineState.
- **Then:** A list of Send objects for each unit descriptor is returned.

### graph.py.should_refine_analysis
- **Given:** A PipelineState object is provided.
- **When:** should_refine_analysis is called with the PipelineState.
- **Then:** A list of Send objects for re-analysis or 'reduce_modules' is returned.

### graph.py.should_revise
- **Given:** A PipelineState object is provided.
- **When:** should_revise is called with the PipelineState.
- **Then:** A string 'assemble_document' or 'END' is returned.

### pipeline/model_catalog.py.list_providers
- **Given:** No preconditions are required.
- **When:** list_providers is called.
- **Then:** A list of tuples containing provider keys and their labels is returned.

### pipeline/model_catalog.py.provider_config
- **Given:** A provider key is provided.
- **When:** provider_config is called with the provider key.
- **Then:** A dictionary containing the configuration details of the specified provider is returned.

### pipeline/model_catalog.py.get_model_options
- **Given:** A provider key is provided.
- **When:** get_model_options is called with the provider key.
- **Then:** A list of tuples containing model options for the specified provider is returned.

### pipeline/model_catalog.py.default_model
- **Given:** A provider key is provided.
- **When:** default_model is called with the provider key.
- **Then:** The default model identifier for the specified provider is returned.

### pipeline/nodes/analyze_unit.py._get_semaphore
- **Given:** A desired size for the semaphore is provided.
- **When:** _get_semaphore is called with the size.
- **Then:** A threading.BoundedSemaphore object with the specified size is returned and _SEM_STATE is modified to store the semaphore and its size.

### pipeline/nodes/analyze_unit.py._elide
- **Given:** Source bytes and a list of byte ranges to be replaced are provided.
- **When:** _elide is called with the source bytes and ranges.
- **Then:** A string with the specified byte ranges replaced by a marker is returned.

### pipeline/nodes/analyze_unit.py._select_source
- **Given:** A unit descriptor containing path and kind information is provided.
- **When:** _select_source is called with the unit descriptor.
- **Then:** A tuple containing a snippet of the source and a focus note is returned.

### pipeline/nodes/analyze_unit.py.analyze_unit
- **Given:** A unit descriptor with analysis parameters is provided.
- **When:** analyze_unit is called with the unit descriptor.
- **Then:** A dictionary containing unit analyses and any mapping errors is returned.

### pipeline/nodes/extract_architecture.py._aggregate_evidence
- **Given:** Unit analyses and a dependency manifest are provided.
- **When:** _aggregate_evidence is called with the unit analyses and dependency manifest.
- **Then:** A dictionary containing aggregated evidence of cross-cutting concerns is returned.

### pipeline/nodes/extract_architecture.py.extract_architecture
- **Given:** A PipelineState object is provided.
- **When:** extract_architecture is called with the PipelineState.
- **Then:** A dictionary containing data schemas and cross-cutting concerns is returned.

### pipeline/nodes/extract_architecture.py._summary
- **Given:** A list of strings and a maximum number of items to include in the summary are provided.
- **When:** _summary is called with the list of strings and the maximum number.
- **Then:** A summarized string is returned.

### pipeline/vectordb.py.get_conn_string
- **Given:** No preconditions are required.
- **When:** get_conn_string is called.
- **Then:** The connection string for the PostgreSQL database is returned.

### pipeline/vectordb.py.get_embeddings
- **Given:** No preconditions are required.
- **When:** get_embeddings is called.
- **Then:** An Embeddings object, either OpenAIEmbeddings or OllamaEmbeddings, is returned.

### pipeline/vectordb.py.collection_for
- **Given:** A repository path is provided.
- **When:** collection_for is called with the repository path.
- **Then:** A stable, unique collection name is returned.

### pipeline/vectordb.py.get_store
- **Given:** A collection name and optional embedding metadata are provided.
- **When:** get_store is called with the collection name and embedding metadata.
- **Then:** A PGVector store for the collection is returned.

### pipeline/vectordb.py._provider_from_dim
- **Given:** An embedding dimension is provided.
- **When:** _provider_from_dim is called with the dimension.
- **Then:** A dictionary containing provider and model information or None is returned.

### pipeline/vectordb.py.collection_embed_config
- **Given:** A collection name is provided.
- **When:** collection_embed_config is called with the collection name.
- **Then:** A dictionary containing provider and model information or None is returned.

### pipeline/vectordb.py._raw_conn_string
- **Given:** No preconditions are required.
- **When:** _raw_conn_string is called.
- **Then:** The raw connection string for psycopg is returned.

### pipeline/vectordb.py.check_connection
- **Given:** No preconditions are required.
- **When:** check_connection is called.
- **Then:** A tuple containing the connection status and a message is returned.

### pipeline/vectordb.py.list_collections
- **Given:** No preconditions are required.
- **When:** list_collections is called.
- **Then:** A list of names of existing collections is returned.

### cli/utils.py.ask_repo_path
- **Given:** No preconditions are required.
- **When:** ask_repo_path is called.
- **Then:** The validated repository path or URL is returned.

### cli/utils.py._valid
- **Given:** An input value is provided.
- **When:** _valid is called with the input value.
- **Then:** True is returned if the input is valid, otherwise an error message is returned.

### cli/utils.py.ask_output_file
- **Given:** The user is prompted for the name of the output file.
- **When:** The user provides a file name or accepts the default.
- **Then:** The function returns the provided file name or 'spec.md' if the default is accepted.

### cli/utils.py.ask_provider
- **Given:** The user is prompted to select an LLM provider from a list of available options.
- **When:** The user selects an LLM provider.
- **Then:** The function returns the selected LLM provider.

### cli/utils.py.ask_model
- **Given:** The user is prompted to select a model for a given provider.
- **When:** The user selects a model or provides a custom input.
- **Then:** The function returns the selected model for the provider and warns if the provider's API key is missing.

### cli/utils.py.ask_ollama_model
- **Given:** The user is prompted to select an Ollama model.
- **When:** The user selects a model or provides a custom input.
- **Then:** The function returns the selected Ollama model.

### cli/utils.py.ask_max_files
- **Given:** The user is prompted to set a file cap for limiting LLM calls.
- **When:** The user sets a file cap or chooses no cap.
- **Then:** The function returns the file cap as an integer or None if no cap is chosen.

### cli/utils.py.ask_skip_tests
- **Given:** The user is prompted to decide whether to skip test files during analysis.
- **When:** The user decides to skip or not skip test files.
- **Then:** The function returns a boolean indicating whether to skip test files.

### cli/utils.py.ask_analysis_passes
- **Given:** The user is prompted to specify the number of analysis refinement passes.
- **When:** The user specifies the number of passes.
- **Then:** The function returns the specified number of analysis refinement passes as an integer.

### state_schema.py.merge_unit_analyses
- **Given:** Two lists of unit analyses are provided.
- **When:** The function is called to merge the lists by unit_id.
- **Then:** The function returns a list of unit analyses where the latest analysis replaces any previous one.

### pipeline/nodes/generate_diagram.py._node_id
- **Given:** A name is provided to convert into a node id.
- **When:** The function is called with the name.
- **Then:** The function returns a stable, Mermaid-safe node id.

### pipeline/nodes/generate_diagram.py._module_label
- **Given:** A module name is provided.
- **When:** The function is called with the module name.
- **Then:** The function returns a label for the module, using '(root)' for the root module.

### pipeline/nodes/generate_diagram.py._clean_label
- **Given:** Text and a limit are provided.
- **When:** The function is called with the text and limit.
- **Then:** The function returns a cleaned and possibly truncated label suitable for use as a Mermaid label.

### pipeline/nodes/generate_diagram.py._module_graph
- **Given:** Lists of modules and entry points are provided.
- **When:** The function is called with the modules and entry points.
- **Then:** The function returns a Mermaid graph definition or None if no modules are provided.

### pipeline/nodes/generate_diagram.py._integrations_graph
- **Given:** Cross-cutting concerns and a system label are provided.
- **When:** The function is called with the cross-cutting concerns and system label.
- **Then:** The function returns a Mermaid graph definition or None if no integrations are provided.

### pipeline/nodes/generate_diagram.py._member_name
- **Given:** A name is provided to sanitize.
- **When:** The function is called with the name.
- **Then:** The function returns a sanitized member name suitable for use in Mermaid class diagrams.

### pipeline/nodes/generate_diagram.py._field_type
- **Given:** A JSON-schema property is provided.
- **When:** The function is called with the property.
- **Then:** The function returns a human-readable type description.

### pipeline/nodes/generate_diagram.py._refs
- **Given:** A JSON-schema property is provided.
- **When:** The function is called with the property.
- **Then:** The function returns a set of schema names referenced by the property.

### pipeline/nodes/generate_diagram.py._data_model
- **Given:** A list of data schema objects is provided.
- **When:** The function is called with the data schemas.
- **Then:** The function returns a Mermaid class diagram or None if no schemas are provided.

### pipeline/nodes/generate_diagram.py.generate_diagram
- **Given:** A PipelineState containing analysis data is provided.
- **When:** The function is called with the PipelineState.
- **Then:** The function returns a dictionary containing the architecture diagram.

### pipeline/llm.py.get_provider
- **Given:** Environment variables are set with the active LLM provider key.
- **When:** The function is called.
- **Then:** The function returns the active LLM provider key.

### pipeline/llm.py.get_model_id
- **Given:** Environment variables or provider defaults are available.
- **When:** The function is called.
- **Then:** The function returns the resolved chat model id.

### pipeline/llm.py.get_llm
- **Given:** a valid temperature value
- **When:** the get_llm function is called with the temperature
- **Then:** an LLM client is instantiated and returned

### pipeline/llm.py._invoke_with_retry
- **Given:** an LLM client and a list of messages
- **When:** the _invoke_with_retry function is called with a maximum of 5 connection retries
- **Then:** the LLM is invoked with retry logic for transient connection errors

### pipeline/llm.py._extract_json_from_text
- **Given:** a string containing JSON with markdown code fences
- **When:** the _extract_json_from_text function is called with the string
- **Then:** the raw JSON text is extracted and returned

### pipeline/llm.py._try_repair_json
- **Given:** a string containing potentially truncated JSON
- **When:** the _try_repair_json function is called with the string
- **Then:** the JSON text is repaired by closing open brackets and braces and returned

### pipeline/llm.py.call_llm_json
- **Given:** a prompt string and a maximum of 2 retries for JSON parsing
- **When:** the call_llm_json function is called with the prompt
- **Then:** a JSON object or array is parsed from the response and returned, with caching

### pipeline/llm.py._call_llm_json_uncached
- **Given:** a prompt string and a maximum of 2 retries for JSON parsing
- **When:** the _call_llm_json_uncached function is called with the prompt
- **Then:** a JSON object or array is parsed from the response and returned without caching

### pipeline/llm.py.call_llm_structured
- **Given:** a prompt string, a pydantic model schema, and a maximum of 2 retries
- **When:** the call_llm_structured function is called with the prompt and schema
- **Then:** a validated instance of the schema is returned, with caching

### pipeline/llm.py._call_llm_structured_uncached
- **Given:** a prompt string, a pydantic model schema, and a maximum of 2 retries
- **When:** the _call_llm_structured_uncached function is called with the prompt and schema
- **Then:** a validated instance of the schema is returned without caching

### pipeline/llm.py.call_llm_text
- **Given:** a prompt string
- **When:** the call_llm_text function is called with the prompt
- **Then:** the raw text response from the LLM is returned, with caching

### pipeline/nodes/review_consistency.py._build_ground_truth
- **Given:** a PipelineState containing analyses and dependencies
- **When:** the _build_ground_truth function is called with the state
- **Then:** a JSON string of verifiable facts is assembled and returned

### pipeline/nodes/review_consistency.py._dep_name
- **Given:** a dependency string with a version specifier
- **When:** the _dep_name function is called with the string
- **Then:** the package name without the version specifier is returned

### pipeline/nodes/review_consistency.py._norm
- **Given:** a text string
- **When:** the _norm function is called with the text
- **Then:** the text is normalized by stripping punctuation and converting to lowercase, and returned

### pipeline/nodes/review_consistency.py._is_symbol_like
- **Given:** a token string
- **When:** the _is_symbol_like function is called with the token
- **Then:** a boolean indicating whether the token resembles a code symbol is returned

### pipeline/nodes/review_consistency.py._collect_known_symbols
- **Given:** a PipelineState
- **When:** the _collect_known_symbols function is called with the state
- **Then:** a set of known symbols from the pipeline state is collected and returned

### pipeline/nodes/review_consistency.py._filter_findings
- **Given:** a list of findings and a PipelineState
- **When:** the _filter_findings function is called with the findings and state
- **Then:** a filtered list of findings that are not contradicted by the ground truth is returned

### pipeline/nodes/review_consistency.py._strip_deterministic
- **Given:** a document string
- **When:** the _strip_deterministic function is called with the document
- **Then:** the document with deterministic content removed is returned

### pipeline/nodes/review_consistency.py.review_consistency
- **Given:** a PipelineState
- **When:** the review_consistency function is called with the state
- **Then:** a dictionary of review results indicating consistency status is returned

### pipeline/artifacts.py.render_class_spec
- **Given:** a UnitAnalysis object
- **When:** the render_class_spec function is called with the object
- **Then:** a Markdown representation of the unit analysis is generated and returned

### pipeline/artifacts.py.render_stage_doc
- **Given:** a node identifier and a state update dictionary
- **When:** the render_stage_doc function is called with the node_id and update
- **Then:** a Markdown document summarizing the results of the pipeline stage is created and returned, or None if not applicable

### pipeline/artifacts.py.write_text
- **Given:** a file path and text content
- **When:** the write_text function is called with the path and content
- **Then:** the content is written to a file at the specified path

### pipeline/nodes/assemble_document.py._build_inventory
- **Given:** a PipelineState containing analysis data
- **When:** the _build_inventory function is called with the state
- **Then:** a formatted inventory of files, languages, entry points, and dependencies is generated and returned

### pipeline/nodes/assemble_document.py._build_interface_index
- **Given:** a PipelineState containing analysis data
- **When:** the _build_interface_index function is called with the state
- **Then:** a formatted index of public interfaces is created and returned

### pipeline/nodes/assemble_document.py._insert_diagram
- **Given:** a document string and a diagram string
- **When:** the _insert_diagram function is called with the document and diagram
- **Then:** the document with the diagram inserted is returned

### pipeline/nodes/assemble_document.py.assemble_document
- **Given:** a PipelineState containing analysis data
- **When:** the assemble_document function is called with the state
- **Then:** a dictionary representing the assembled document with all relevant sections is returned

### pipeline/parsers/tree_sitter_parser.py._get_lang_from_path
- **Given:** a file path string
- **When:** the _get_lang_from_path function is called with the path
- **Then:** the programming language name is determined from the file extension and returned, or None if not found

### pipeline/parsers/tree_sitter_parser.py._get_ts_parser
- **Given:** a language string
- **When:** the _get_ts_parser function is called with the language
- **Then:** a Tree-sitter parser for the given language is retrieved and returned, or None if unavailable

### pipeline/parsers/tree_sitter_parser.py._extract_go_imports
- **Given:** Go source code as a string
- **When:** the _extract_go_imports function is called with the source code
- **Then:** a list of Go import paths is extracted and returned

### pipeline/parsers/tree_sitter_parser.py._extract_imports_regex
- **Given:** source code as a string and a language string
- **When:** the _extract_imports_regex function is called with the source code and language
- **Then:** a list of import paths is extracted using regex patterns specific to the language and returned

### pipeline/parsers/tree_sitter_parser.py._count_loc
- **Given:** source code as a string
- **When:** the _count_loc function is called with the source code
- **Then:** the number of non-blank lines in the source code is counted and returned

### pipeline/parsers/tree_sitter_parser.py._structure
- **Given:** lists of function names, class names, import statements, and a lines of code count
- **When:** the _structure function is called with the functions, classes, imports, and loc
- **Then:** a structured representation of the code is built and returned

### pipeline/parsers/tree_sitter_parser.py._empty_structure
- **Given:** a source code string with 3 lines
- **When:** the _empty_structure function is called with the source code
- **Then:** a dictionary with a LOC count of 3 is returned

### pipeline/parsers/tree_sitter_parser.py._first_name
- **Given:** an AST node representing a function declaration and the source code in bytes
- **When:** the _first_name function is called with the node and source bytes
- **Then:** the name of the function declaration is returned as a string

### pipeline/parsers/tree_sitter_parser.py._traverse_for_names
- **Given:** an AST node representing a class with methods, source code in bytes, and empty lists for functions and classes
- **When:** the _traverse_for_names function is called with the node, source bytes, and the lists
- **Then:** the function names are added to the functions list and the class name is added to the classes list

### pipeline/parsers/tree_sitter_parser.py._collect_class_spans
- **Given:** an AST node representing a top-level class, source code in bytes, and an empty list for output
- **When:** the _collect_class_spans function is called with the node, source bytes, and the output list
- **Then:** the byte and line spans of the class are added to the output list

### pipeline/parsers/tree_sitter_parser.py.extract_class_spans
- **Given:** a file path to a source code file containing top-level classes
- **When:** the extract_class_spans function is called with the file path
- **Then:** a list of dictionaries with byte and line span information for each top-level class is returned

### pipeline/nodes/ingest.py._resolve_dependencies
- **Given:** a list of FileMeta objects with unresolved dependencies
- **When:** the _resolve_dependencies function is called with the list
- **Then:** the FileMeta.depends_on attribute for each file is modified to reflect resolved dependencies

### pipeline/nodes/ingest.py._exists
- **Given:** a base path and a tuple of file extensions
- **When:** the _exists function is called with the base path and extensions
- **Then:** the resolved path is returned if a file with one of the extensions exists, otherwise None is returned

### pipeline/nodes/ingest.py._resolve_one
- **Given:** an import statement, a programming language, and a file directory
- **When:** the _resolve_one function is called with the import statement, language, and directory
- **Then:** the resolved file path is returned if it exists, otherwise None is returned

### pipeline/nodes/ingest.py._classify_kind
- **Given:** a relative file path and a file extension
- **When:** the _classify_kind function is called with the path and extension
- **Then:** the FileKind classification of the file is returned

### pipeline/nodes/ingest.py._extract_imports
- **Given:** source code and a programming language
- **When:** the _extract_imports function is called with the source code and language
- **Then:** a list of extracted import statements is returned

### pipeline/nodes/ingest.py._parse_requirements_txt
- **Given:** a path to a Python requirements.txt file
- **When:** the _parse_requirements_txt function is called with the path
- **Then:** a dictionary containing type and dependencies is returned

### pipeline/nodes/ingest.py._parse_package_json
- **Given:** a path to a Node.js package.json file
- **When:** the _parse_package_json function is called with the path
- **Then:** a dictionary containing type, name, version, dependencies, and devDependencies is returned

### pipeline/nodes/ingest.py._parse_go_mod
- **Given:** a path to a Go go.mod file
- **When:** the _parse_go_mod function is called with the path
- **Then:** a dictionary containing module name and dependencies is returned

### pipeline/nodes/ingest.py._parse_cargo_toml
- **Given:** a path to a Rust Cargo.toml file
- **When:** the _parse_cargo_toml function is called with the path
- **Then:** a dictionary containing dependencies is returned

### pipeline/nodes/ingest.py._parse_pom_xml
- **Given:** a path to a Java pom.xml file
- **When:** the _parse_pom_xml function is called with the path
- **Then:** a dictionary containing dependencies is returned

### cli/main.py.PipelineBuffer.__init__
- **Given:** a default or specified maximum number of messages
- **When:** the PipelineBuffer is initialized
- **Then:** the message buffer and node status tracking are initialized

### cli/main.py.PipelineBuffer.add_message
- **Given:** a PipelineBuffer instance and a message type and content
- **When:** the add_message method is called with the message type and content
- **Then:** a timestamped message is appended to the message buffer

### cli/main.py.PipelineBuffer.set_node
- **Given:** a PipelineBuffer instance, a node identifier, and a status
- **When:** the set_node method is called with the node identifier and status
- **Then:** the status of the node is updated and set as the current node if in progress

### cli/main.py.PipelineBuffer.complete_node
- **Given:** a PipelineBuffer instance and a node identifier
- **When:** the complete_node method is called with the node identifier
- **Then:** the node is marked as completed

### cli/main.py._safe_name
- **Given:** a unit path string
- **When:** the _safe_name function is called with the path
- **Then:** a filename-safe string derived from the path is returned

### cli/main.py._pause
- **Given:** The program is running and requires user interaction to continue.
- **When:** The _pause function is called with a specific message.
- **Then:** The program displays the message and waits for the user to press Enter to continue.

### cli/main.py._render_doc
- **Given:** The terminal UI is ready to display content.
- **When:** The _render_doc function is called with a title and content.
- **Then:** The terminal UI renders the document with the specified title and content.

### cli/main.py.interactive_viewer
- **Given:** The pipeline execution is complete and documents are available for viewing.
- **When:** The interactive_viewer function is called with the final document.
- **Then:** The user is able to browse through the final document in the terminal UI.

### cli/main.py.run_pipeline
- **Given:** The pipeline is configured and ready to execute.
- **When:** The run_pipeline function is called with no_cache set to true.
- **Then:** The pipeline executes without using any cached data.

### cli/main.py.run_pipeline
- **Given:** The pipeline is configured and ready to execute.
- **When:** The run_pipeline function is called with clear_cache set to true.
- **Then:** The pipeline executes and clears any existing cache before execution.

### pipeline/nodes/route_units.py::_file_units
- **Given:** Generate unit descriptors for a source file, splitting large files into class-based units if possible.
- **When:** _file_units is called with file_meta: object — metadata of the file, repo_path: str — path to the repository, max_parallelism: int — maximum parallelism allowed
- **Then:** it produces list[dict] — list of unit descriptors

### pipeline/repo_source.py::_clone_dir_name
- **Given:** Generate a stable directory name from a git URL for cloning purposes.
- **When:** _clone_dir_name is called with url: str — the git URL to process
- **Then:** it produces str — a stable, filesystem-safe directory name derived from the URL

### pipeline/repo_source.py::_normalize_url
- **Given:** Normalize a git URL, converting shorthand forms to full URLs.
- **When:** _normalize_url is called with url: str — the URL to normalize
- **Then:** it produces str — the normalized URL

### cli/chat.py::_format_context
- **Given:** Format document contents with metadata labels for display.
- **When:** _format_context is called with docs: iterable — collection of document objects with metadata
- **Then:** it produces str — formatted string of document contents with labels

### cli/chat.py::_sources_line
- **Given:** Generate a unique list of document tags from metadata for citation.
- **When:** _sources_line is called with docs: iterable — collection of document objects with metadata
- **Then:** it produces str — comma-separated list of unique document tags

### pipeline/indexer.py::_chunk
- **Given:** Split a given text into chunks of a specified size with overlap to facilitate processing without external dependencies.
- **When:** _chunk is called with text: str — the text to be chunked, size: int — the maximum size of each chunk, overlap: int — the number of overlapping characters between chunks
- **Then:** it produces list[str] — list of text chunks

### pipeline/indexer.py::_split_spec_sections
- **Given:** Divide a specification document into sections based on top-level headings for structured processing.
- **When:** _split_spec_sections is called with spec: str — the specification text to be split
- **Then:** it produces list[tuple[str, str]] — list of tuples containing section titles and bodies

### graph.py::build_graph
- **Given:** Construct and compile the state graph for the code-to-spec pipeline.
- **When:** build_graph is called with checkpointer: optional — a checkpointing mechanism
- **Then:** it produces StateGraph — the compiled state graph for the pipeline

### state_schema.py::FileKind
- **Given:** Enumerate different kinds of files in a codebase, such as source, test, config, etc.
- **When:** FileKind is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::FileMeta
- **Given:** Represent metadata about a file, including its path, language, kind, and dependencies.
- **When:** FileMeta is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::UnitAnalysis
- **Given:** Represent the output of a map step for an analyzable unit, detailing its purpose and interfaces.
- **When:** UnitAnalysis is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::UnitAnalysisResult
- **Given:** Capture the intent fields of an analysis unit, separate from deterministic fields.
- **When:** UnitAnalysisResult is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::ModuleSummary
- **Given:** Summarize a module's responsibilities and public surface after an intermediate reduce step.
- **When:** ModuleSummary is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::DataSchema
- **Given:** Define a language-neutral type description using JSON schema.
- **When:** DataSchema is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::CrossCutting
- **Given:** Capture cross-cutting concerns like error handling and logging in the system.
- **When:** CrossCutting is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::BehavioralTest
- **Given:** Define language-neutral acceptance criteria for reimplementation.
- **When:** BehavioralTest is called with valid inputs
- **Then:** it produces the documented result

### state_schema.py::PipelineState
- **Given:** Represent the state of the pipeline, including inputs, outputs, and analysis progress.
- **When:** PipelineState is called with valid inputs
- **Then:** it produces the documented result

### pipeline/parsers/tree_sitter_parser.py::parse_file
- **Given:** Parse a file to extract its structure using Tree-sitter.
- **When:** parse_file is called with path: str — the file path to parse
- **Then:** it produces the documented result

### cli/main.py::_default
- **Given:** Invoke the startup menu if no subcommand is provided.
- **When:** _default is called with ctx: typer.Context — the context of the Typer application
- **Then:** it produces the documented result

### cli/main.py::create_layout
- **Given:** Set up the layout structure for the terminal UI.
- **When:** create_layout is called with valid inputs
- **Then:** it produces Layout — the configured layout for the terminal UI

### cli/main.py::update_display
- **Given:** Refresh the terminal UI with the latest pipeline progress and messages.
- **When:** update_display is called with layout: Layout — the layout to update with current information
- **Then:** it produces the documented result

### cli/main.py::get_user_selections
- **Given:** Collect user input for pipeline configuration through an interactive wizard.
- **When:** get_user_selections is called with valid inputs
- **Then:** it produces dict — user selections for pipeline configuration

### cli/main.py::step_box
- **Given:** Display a step box with a title and hint in the terminal UI.
- **When:** step_box is called with title: str — the title of the step, hint: str — a hint or description for the step
- **Then:** it produces the documented result

### cli/main.py::_persist_class_spec
- **Given:** Store the per-class specification in the buffer and on disk.
- **When:** _persist_class_spec is called with ua — the unit analysis object
- **Then:** it produces the documented result

### cli/main.py::_persist_stage_doc
- **Given:** Store the per-stage documentation in the buffer and on disk.
- **When:** _persist_stage_doc is called with node_id: str — the identifier of the pipeline stage, update: dict — the update data for the stage
- **Then:** it produces the documented result
